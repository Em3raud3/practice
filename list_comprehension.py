nums = [n for n in range(1, 11)]

# my_list = []
# for n in nums:
#     my_list.append(n*n)
# print(my_list)

# my_list = [n*n for n in nums]
# print(my_list)

# * my_list = [n for n in nums if n % 2 == 0]
#
# * my_list = [(letter, num) for letter in "abcd" for num in range(4)]
# * print(my_list)

# ! Dictionary example *************************************************

# ? names = ["Bruce", "clark", "Peter", "Logan", "Wade"]
# ? heros = ["Batman", "Superman", "Spiderman", "Wolverine", "Deadpool"]
# ? name_list = [name for name in zip(names, heros)]

# ? my_dict = {name: hero for name, hero in name_list if name != "Peter"}
# ? print(my_dict)


# nums = [1, 1, 2, 1, 3, 4, 5, 5, 6, 8, 7, 9, 9]

# my_set = {num for num in nums if num % 2 == 0}
# print(my_set)
